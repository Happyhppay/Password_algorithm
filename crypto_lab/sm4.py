"""SM4 block cipher with ECB/CBC helpers."""

from __future__ import annotations

from .utils import pkcs7_pad, pkcs7_unpad, split_blocks, xor_bytes

BLOCK_SIZE = 16
SBOX = [
    ,
]
FK = [0xA3B1BAC6, 0x56AA3350, 0x677D9197, 0xB27022DC]
CK = [
    0x00070E15, 0x1C232A31, 0x383F464D, 0x545B6269, 0x70777E85, 0x8C939AA1, 0xA8AFB6BD, 0xC4CBD2D9,
    0xE0E7EEF5, 0xFC030A11, 0x181F262D, 0x343B4249, 0x50575E65, 0x6C737A81, 0x888F969D, 0xA4ABB2B9,
    0xC0C7CED5, 0xDCE3EAF1, 0xF8FF060D, 0x141B2229, 0x30373E45, 0x4C535A61, 0x686F767D, 0x848B9299,
    0xA0A7AEB5, 0xBCC3CAD1, 0xD8DFE6ED, 0xF4FB0209, 0x10171E25, 0x2C333A41, 0x484F565D, 0x646B7279,
]


def _rotl(x: int, n: int) -> int:
    return ((x << n) | (x >> (32 - n))) & 0xFFFFFFFF


def _tau(a: int) -> int:
    b = 0
    for shift in (24, 16, 8, 0):
        b = (b << 8) | SBOX[(a >> shift) & 0xFF]
    return b


def _l_trans(b: int) -> int:
    return b ^ _rotl(b, 2) ^ _rotl(b, 10) ^ _rotl(b, 18) ^ _rotl(b, 24)


def _l_key(b: int) -> int:
    return b ^ _rotl(b, 13) ^ _rotl(b, 23)


def _t(x: int) -> int:
    return _l_trans(_tau(x))


def _t_key(x: int) -> int:
    return _l_key(_tau(x))


def _words(block: bytes) -> list[int]:
    return [int.from_bytes(block[i : i + 4], "big") for i in range(0, 16, 4)]


def _from_words(words: list[int]) -> bytes:
    return b"".join((w & 0xFFFFFFFF).to_bytes(4, "big") for w in words)


def round_keys(key: bytes) -> list[int]:
    if len(key) != 16:
        raise ValueError("SM4 key must be 16 bytes")
    mk = _words(key)
    k = [mk[i] ^ FK[i] for i in range(4)]
    keys = []
    for i in range(32):
        value = k[i] ^ _t_key(k[i + 1] ^ k[i + 2] ^ k[i + 3] ^ CK[i])
        k.append(value)
        keys.append(value)
    return keys


def encrypt_block(block: bytes, key: bytes) -> bytes:
    if len(block) != BLOCK_SIZE:
        raise ValueError("SM4 block must be 16 bytes")
    x = _words(block)
    for rk in round_keys(key):
        x.append(x[-4] ^ _t(x[-3] ^ x[-2] ^ x[-1] ^ rk))
    return _from_words([x[35], x[34], x[33], x[32]])


def decrypt_block(block: bytes, key: bytes) -> bytes:
    if len(block) != BLOCK_SIZE:
        raise ValueError("SM4 block must be 16 bytes")
    x = _words(block)
    for rk in reversed(round_keys(key)):
        x.append(x[-4] ^ _t(x[-3] ^ x[-2] ^ x[-1] ^ rk))
    return _from_words([x[35], x[34], x[33], x[32]])


def encrypt(data: bytes, key: bytes, mode: str = "CBC", iv: bytes | None = None) -> bytes:
    mode = mode.upper()
    padded = pkcs7_pad(data, BLOCK_SIZE)
    if mode == "ECB":
        return b"".join(encrypt_block(block, key) for block in split_blocks(padded, BLOCK_SIZE))
    if mode != "CBC":
        raise ValueError("SM4 mode must be ECB or CBC")
    if iv is None:
        iv = bytes(BLOCK_SIZE)
    if len(iv) != BLOCK_SIZE:
        raise ValueError("SM4-CBC IV must be 16 bytes")
    out, prev = [], iv
    for block in split_blocks(padded, BLOCK_SIZE):
        enc = encrypt_block(xor_bytes(block, prev), key)
        out.append(enc)
        prev = enc
    return b"".join(out)


def decrypt(data: bytes, key: bytes, mode: str = "CBC", iv: bytes | None = None) -> bytes:
    mode = mode.upper()
    if mode == "ECB":
        return pkcs7_unpad(b"".join(decrypt_block(block, key) for block in split_blocks(data, BLOCK_SIZE)), BLOCK_SIZE)
    if mode != "CBC":
        raise ValueError("SM4 mode must be ECB or CBC")
    if iv is None:
        iv = bytes(BLOCK_SIZE)
    if len(iv) != BLOCK_SIZE:
        raise ValueError("SM4-CBC IV must be 16 bytes")
    out, prev = [], iv
    for block in split_blocks(data, BLOCK_SIZE):
        dec = xor_bytes(decrypt_block(block, key), prev)
        out.append(dec)
        prev = block
    return pkcs7_unpad(b"".join(out), BLOCK_SIZE)
